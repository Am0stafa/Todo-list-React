import React from 'react';
import '../Layout/header.style.css'

const Header = (props) => {

    return (
        <div>
            <header className="header" >
            <h1> Todo list </h1>
            </header>
        </div>
    );
}

export default Header;
